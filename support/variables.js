
// CSS selector for the logo button
const LOGO_BUTTON_SELECTOR = '.header2021-logo'

// CSS selector for the search input field
const SEARCH_INPUT_SELECTOR = 'input[type=search]';

// CSS selector for the search button
const SEARCH_BUTTON_SELECTOR = '.header2021-search-button';

// CSS selector for the search result item
const SEARCH_RESULT_ITEM_SELECTOR = '.item-info'

// CSS selector for the search result item list
const SEARCH_RESULT_LIST_SELECTOR = '.list-wrap'

// CSS selector for the deals tab button
const DEALS_BUTTON_SELECTOR = '#trendingBanner_720202';

// CSS selector for the deals header
const DEALS_HEADER_SELECTOR = '.limit-string';

// CSS selector for the deals item
const DEALS_ITEM_SELECTOR =  '.item-cell';

// Link text selector for the searched product
const ITEM_IMG_SELECTOR = "*=Microsoft Windows";

// CSS selector for the add to cart button
const ADD_TO_CART_BUTTON_SELECTOR = 'button.btn.btn-wide:has(i.fas.fa-caret-right)';

// CSS selector for the shopping cart icon
const CART_ICON_SELECTOR = 'i.ico-shopping-cart[aria-label="icon of shopping cart"]';

// CSS selector for the remove button
const DELETE_BUTTON_SELECTOR = 'button.btn.btn-mini.btn-tertiary > i.fa.fa-trash[aria-label="trash"]';

// CSS selector for the shopping cart item
const SHOPPING_CART_ITEM_SELECTOR = ".row-body";

// CSS selector for the item added popup
const ITEM_ADDED_POPUP = '.modal-content'

// CSS selector for the popup header
const ITEM_ADDED_HEADER = '.modal-header'

// CSS selevtor for the removed item container
const ITEM_REMOVED_CONTAINER = '.item-container'

// String variable for message comparision on product page
const ITEM_ADDED_TEXT = 'ITEM HAS BEEN ADDED TO CART.'

// String variable for url comparision on shopping cart page
const SHOPPING_CART_URL_ELEMENT = 'shop/cart'

// String variable for url comparison on deals subpage
const DEALS_URL_ELEMENT = 'todays-deals'

// String variable for title comparision on deals subpage
const DEALS_TITLE_TEXT = "TODAY'S BEST DEALS"

// String variable for url comparision on product page
const PRODUCT_URL_ELEMENT = "windows"

// String variable for removal message comparision on cart subpage
const REMOVED_MESSAGE_TEXT = 'Removed:'


module.exports = {
    LOGO_BUTTON_SELECTOR,
    SEARCH_INPUT_SELECTOR,
    SEARCH_BUTTON_SELECTOR,
    SEARCH_RESULT_ITEM_SELECTOR,
    SEARCH_RESULT_LIST_SELECTOR,
    DEALS_BUTTON_SELECTOR,
    DEALS_HEADER_SELECTOR,
    DEALS_ITEM_SELECTOR,
    ITEM_IMG_SELECTOR,
    ADD_TO_CART_BUTTON_SELECTOR,
    CART_ICON_SELECTOR,
    DELETE_BUTTON_SELECTOR,
    SHOPPING_CART_ITEM_SELECTOR,
    DEALS_URL_ELEMENT,
    DEALS_TITLE_TEXT,
    PRODUCT_URL_ELEMENT,
    ITEM_ADDED_HEADER,
    ITEM_ADDED_POPUP,
    ITEM_ADDED_TEXT,
    REMOVED_MESSAGE_TEXT,
    SHOPPING_CART_URL_ELEMENT,
    ITEM_REMOVED_CONTAINER,
    PRODUCT_URL_ELEMENT
};